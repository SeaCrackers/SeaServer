{
  "name": "seaserver",
  "version": "1.0.0",
  "description": "A simple websocket server to de the relay between clients (hosts and palyers)",
  "main": "dist/app.js",
  "scripts": {
    "start": "node dist/app.js",
    "prestart": "npx tsc"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "typescript": "^5.3.3"
  },
  "dependencies": {
    "dotenv": "^16.3.1",
    "socket.io": "^4.7.4"
  }
}
